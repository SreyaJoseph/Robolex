specVersion: "v1"
serviceId: "miniproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "miniproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
